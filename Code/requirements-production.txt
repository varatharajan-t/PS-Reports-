# Production Requirements for PS Reports
# Install with: pip install -r requirements-production.txt
#
# Pins target Python 3.14. numpy 1.x, pandas 2.1, Pillow 10 and psycopg2-binary
# 2.9.9 publish no cp314 wheels and fall back to a source build, so they are not
# usable here. pandas is held below 3.0: that release turns on copy-on-write and
# the new string dtype by default, which changes report output.

# Django Core
Django==5.2.8

# Database - PostgreSQL for Production
psycopg2-binary==2.9.12

# Excel Processing
openpyxl==3.1.5
pandas==2.3.3

# Data Processing
numpy==2.5.1

# Environment Variables & Security
python-decouple==3.8
python-dotenv==1.2.2

# File Handling
Pillow==12.3.0

# Web Server - Production WSGI Server
gunicorn==26.0.0
whitenoise==6.12.0

# Django Packages
django-crispy-forms==2.6
crispy-bootstrap5==2026.3

# Security & Monitoring (Optional but Recommended)
# Uncomment these for enhanced production security and monitoring:

# Error Tracking
# sentry-sdk==1.39.1

# Security Headers
# django-csp==3.7
# django-permissions-policy==4.18.0

# Rate Limiting
# django-ratelimit==4.1.0

# Additional Production Recommendations:
# - Configure Sentry for error tracking and monitoring
# - Use Redis for caching and session storage
# - Enable django-csp for Content Security Policy headers
# - Consider django-axes for brute-force protection

# Optional: Async Task Processing (if needed for background jobs)
# celery==5.3.4
# redis==5.0.1
# django-celery-beat==2.5.0

# Optional: API Support (if exposing REST API)
# djangorestframework==3.14.0
# django-cors-headers==4.3.1
# django-filter==23.5
